% Model Builder 4.2 report marker -- not a runtime template.
% SOURCE OF TRUTH / FUENTE CANONICA / MASSGEBLICHE QUELLE:
% report_generator.py owns the single LaTeX structure and obtains all visible
% presentation text from the canonical UTF-8 catalogs in i18n/{es,en,de}.json.
% It includes DIN 6892/FVA requested, derived, realised and standards evidence.
% Editing this marker does not change generated reports; it exists only so the
% packaged resources state that ownership unambiguously and can be audited.
% Los informes se generan en el idioma persistido en project.language.
% Berichte werden in der unter project.language gespeicherten Sprache erzeugt.
\ProvidesFile{report_template.tex}[2026/09/11 Model Builder 4.2 source-of-truth marker]
